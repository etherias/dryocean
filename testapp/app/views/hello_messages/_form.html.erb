<%= form_for(@hello_message) do |f| %>
  <% if @hello_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hello_message.errors.count, "error") %> prohibited this hello_message from being saved:</h2>

      <ul>
      <% @hello_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
  </div>
  <div class="field">
    <%= f.label :times_shown %><br>
    <%= f.number_field :times_shown %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
